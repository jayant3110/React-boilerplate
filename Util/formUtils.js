// This file contains utils that can are common to all the forms.
// export const getLabelClass = (error, length, flag) => {
//   if (error || flag) {
//     return 'form-label-danger';
//   } // else
//   return length ? 'form-label-active' : 'form-label-inactive';
// };

// export const getAcceleratorLabelClass = (error, length, flag) => {
//   if (error || flag) {
//     return 'form-label-danger';
//   } // else
//   return length ? 'p1-typo' : 'p1-typo';
// };

// export const getLabel = (error, text, flag, head) => {
//   if (error) {
//     return error;
//     // eslint-disable-next-line no-else-return
//   } else if (flag) {
//     return head;
//   } else {
//     return text;
//   }
// };
